@{
    ViewData["Title"] = "Home page";
}

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home page</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" />
    <style>
        body {
            background-color: #fce4ec; 
            color: #333;
            font-family: 'Arial', sans-serif;
            min-height: 100vh; 
            display: flex;
            flex-direction: column;
        }

        .hero-section {
            background-color: #9898; 
            color: white;
            padding: 100px 0;
            text-align: center;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

            .hero-section h1 {
                font-size: 3.5rem;
                font-weight: bold;
            }

            .hero-section p {
                font-size: 1.2rem;
                margin-top: 20px;
            }

        .btn-custom {
            background-color: #5656; 
            color: white;
            padding: 10px 20px;
            font-size: 1rem;
            border-radius: 30px;
            text-decoration: none;
            margin-top: 20px;
            transition: background-color 0.3s ease;
        }

            .btn-custom:hover {
                background-color: #5656; 
                transform: translateY(-2px);
                color: #5656; 
            }

        .content-section {
            padding: 30px 0; /* Reducir padding superior e inferior */
            text-align: center;
            flex-grow: 1; /* Permite que esta sección crezca y ocupe el espacio disponible */
        }

            .content-section img {
                max-width: 400px; /* Ajusta el tamaño de la imagen al 50% del contenedor */
                height: 200px; /* Mantiene la proporción */
                margin-top: 10px;
            }

            .content-section h2 {
                font-size: 2rem;
                color: #2323; 
                margin-bottom: 50px; /* Reducir espacio entre el título y la imagen */
            }
    </style>
</head>
<body>

    <div class="hero-section">
        <h1 class="display-4">¡Bienvenido a tu aplicación ASP.NET Core!</h1>
        <p>Descubre cómo crear aplicaciones web modernas con <a href="https://learn.microsoft.com/aspnet/core" target="_blank" style="color: #ffeb3b;">ASP.NET Core</a>.</p>
        <a href="https://learn.microsoft.com/aspnet/core" class="btn-custom">Empezar ahora</a>
    </div>

    <div class="content-section">
        <h2>Conoce más sobre ASP.NET Core</h2>
        <p>ASP.NET Core es un framework de desarrollo de aplicaciones web de alto rendimiento, multiplataforma y de código abierto.</p>
        <!-- Imagen centrada y ajustada -->
        <img src="~/images/Logo.jpg" />
    </div>

</body>
</html>